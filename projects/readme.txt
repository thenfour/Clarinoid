BASSOONOID = ..
CLARINOID2 = ..
TESTDEVICE = ..

ClarinoidSimulator = windows native graphical sim
ClarinoidTests = unit tests
GenerateModCurveLUT = should not need to be run again; actually should be converted to happening at runtime so no LUT build step is needed.
gfxutils = native win32 gui app for developing knob rendering
serialization = ??
test = old test app
